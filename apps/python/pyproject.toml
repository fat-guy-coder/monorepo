[project]
name = "python-learning"
version = "0.1.0"
description = "Python 学习项目"
requires-python = ">=3.10"
dependencies = [
    "Django>=5.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
